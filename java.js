let doIt = () => {
    x = document.getElementById("bw").value;  // Get the value of the input with id "bw"
    alert(x);  // Show the baggage weight entered in an alert
    alert("done");  // Show a "done" message in an alert

}
